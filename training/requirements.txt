# pip install -r app/requirements.txt -r training/requirements.txt
accelerate>=0.26.0
scikit-learn>=1.3
datasets>=3.0  # 2.x cannot read caches written by newer versions
sentence-transformers>=3.0,<5  # translation similarity filter; 6.x requires transformers 5
